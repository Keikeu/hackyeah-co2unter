// const cityPopulation = 766000;
const cityPopulation = 1;

export const emissions = {
  energy_consumption: {
    min: 400,
    max: 2500,
    step: 100,
    unit: "kWh per month",
    average: 1800,
    factor: 0.385 * 12 * cityPopulation,
  },
  eating_out: {
    min: 0,
    max: 7,
    step: 1,
    unit: "days per week",
    average: 3,
    factor: ((80 * 52) / 7) * cityPopulation,
  },
  private_car_usage: {
    min: 0,
    max: 400,
    step: 10,
    unit: "km per week",
    average: 150,
    factor: 0.192 * 52 * cityPopulation,
  },
  waste_generation: {
    min: 0,
    max: 100,
    step: 5,
    unit: "kg per month",
    average: 40,
    factor: 1.8 * 12 * cityPopulation,
  },
  home_heating: {
    min: 0,
    max: 6000,
    step: 100,
    unit: "kWh per year",
    average: 3000,
    factor: 0.225 * cityPopulation,
  },
  meat_consumption: {
    min: 0,
    max: 7,
    step: 1,
    unit: "days per week",
    average: 5,
    factor: ((16 * 52) / 7) * cityPopulation,
  },
  flights_per_year: {
    min: 0,
    max: 24,
    step: 1,
    unit: "flights per year",
    average: 2,
    factor: 250 * cityPopulation,
  },
  shopping_clothes: {
    min: 0,
    max: 50,
    step: 1,
    unit: "items per year",
    average: 20,
    factor: 15 * cityPopulation,
  },
  public_transport_usage: {
    min: 0,
    max: 14,
    step: 1,
    unit: "days per week",
    average: 5,
    factor: 0.065 * 52 * cityPopulation,
  },
  water_usage: {
    min: 40,
    max: 200,
    step: 5,
    unit: "liters per day",
    average: 150,
    factor: 0.00035 * 365 * cityPopulation,
  },
};

export const EMISSION_LABELS = {
  energy_consumption: "Energy Consumption",
  eating_out: "Eating Out",
  private_car_usage: "Private Car Usage",
  public_transport_usage: "Public Transport Usage",
  flights_per_year: "Flights",
  home_heating: "Home Heating",
  waste_generation: "Waste Generation",
  shopping_clothes: "Shopping Clothes",
  water_usage: "Water Usage",
  meat_consumption: "Meat Consumption",
};

export const EMISSION_COLORS = {
  energy_consumption: "#ac9bdf",
  eating_out: "#ffb74d",
  private_car_usage: "#4db6ac",
  public_transport_usage: "#81c784",
  flights_per_year: "#ff8a65",
  home_heating: "#7986cb",
  waste_generation: "#ffcc80",
  shopping_clothes: "#9575cd",
  water_usage: "#64b5f6",
  meat_consumption: "#e57373",
};
